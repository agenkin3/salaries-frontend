// class Post {
//   constructor(title, salary, detail) {
//     this.title = title; 
//     this.salary = salary;
//     this.detail = detail;
//   }

  // function fetchPosts() {
  //   fetch(${BASE_URL}/users)
  //   .then (resp => resp.json()}
  //   .then (users => {

  //   })

  PostApi.getPosts()